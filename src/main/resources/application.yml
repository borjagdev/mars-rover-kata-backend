# Spring
spring:
    application:
        name: Mars Rover Kata - Backend
    # Database
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}
        username: ${MYSQL_ROOT_USER}
        password: ${MYSQL_ROOT_PASSWORD}